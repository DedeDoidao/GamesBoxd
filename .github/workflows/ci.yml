name: CI

on: 
  push:
    branches:
      - master

jobs: 
  build: 
    runs-on: ubuntu-latest
    
    - name: API Key
      env:
        API_KEY: ${{ secrets.API_KEY }}
      run: |
        echo "Using API Key: $API_KEY"
